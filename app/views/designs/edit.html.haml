.container-fluid.bg-secondary
  .container
    .d-flex
      %div
        -# TODO: i18n
        %span.badge.badge-primary.badge-pill.mt-4 Tasarımı Düzenle
      .ml-auto
        -# TODO: i18n
        %a.badge.badge-primary.badge-pill.mt-4{ href: design_path(@design) } Görüntüle
    %h5.display-4.card-title.mt-3 #{@design.name}
= render 'form'
